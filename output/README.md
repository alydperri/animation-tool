# Output

Approved final animation assets are written here.

Each completed animation gets its own folder:

```text
output/<animation-name>/
├── <animation-name>.gif
└── <animation-name>-spritesheet.png
```

Temporary contact sheets, sliced frames, transparent intermediates, and rejected
generations belong in `work/`, not here.
